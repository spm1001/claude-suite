# Handoff — 2026-08-17

session_id: 8aae2383-5281-4a14-830a-67d466fd5e04
purpose: Board-sweep day — 13 items closed across five suite publishes (1.66.1–1.66.5): ardoise, hublot, and the sharing scanner all fixed and verified
format: fond-v1

## For the next Claude

### Done
- **Cleared the 1.48.1 render-verify debt at open**: the deglacer quirk bullet ("non-default CLAUDE_CONFIG_DIR blinds it — destructively") renders in the live skill, having survived the 1.48.1→1.66.0 churn.
- **Ardoise triple closed** (trousse-jiluru, -fawufi, -rozoso → 1.66.1): SKILL.md fallback recipes (ardoise + hublot) `sort -r` → `sort -rV` — lexicographic sort resolved 1.8.7 above 1.66.0 on the live 89-sibling cache; confirmed portable on the Mac's BSD sort. Print mode honours START_DIR and gains `--cwd DIR` (spec2 reference patch adopted); verified via fake-claude probes on all four shapes plus one real spawn reading its own cwd.
- **skill-forge checklist gained the two banc writing rules** (trousse-vamawu, -muhejo → 1.66.2): rules carry their why (far-transfer 15/15 vs 8/15, p=0.003); reference skills end with a non-exhaustiveness license (flagships 0/5 → 5/5).
- **Hublot trio closed** (trousse-vurojo, -nibajo, -pezogu → 1.66.3): stop() clears the composer with C-u before /exit (proven live — CC's own "Ctrl+Y to paste deleted text" receipt, no stray submit); keys settles 1s before its Enter; SKILL.md learned the key verb, the TUI-menu recipe (/hooks source labels answer scope questions), and the ghost-text hazard row.
- **scan.py de-vacuoused at two layers** (trousse-bujuta → 1.66.4; config layer knocked out at close on Sameer's word → 1.66.5): excludes now match relative to the scan root (absolute-path matching had excluded everything under ~/.claude by ancestry — .claude is itself an exclude); explicit file args scan; 0 files = exit 2 SCANNER FAILURE. Then the second layer: default config discovery (~/.claude/sharing-scan.json, estate copy written and pushed in the .claude repo), inert-category warnings on stderr, config provenance line, and 8 HOME-hermetic regression tests in tests/test_scan.py. Estate control fired on all three config-armed categories.
- **skill-forge grew P9 and the DAAF rows** (trousse-ferupi, -wowujo → 1.66.4): floors-not-fences in register-principles + a Workflow checklist row (attention-narrowing, 0%-vs-100% inbox eval); per-claim provenance flags and the curate-the-pinned-environment discriminator, with deglacer audited as the worked example and cleared.
- **cowork-cloud.md landed** (trousse-susoni → 1.66.4): through the fixed scanner, session/container IDs redacted to shapes, cross-linked both ways with cowork-environment.md, both cowork refs added to the References list. coup-doeil verdict: never built, on no board — the reference stands alone. Notes source now points here as canonical.
- **Filed**: trousse-vosujo — the (user)-tag conflict (lint_skill.py demands it; the cowork-cloud reference records that Cowork silently drops skills carrying it).

### Reflection
- Consolidation paid twice: three field reports → two bugs → one pass, for both the ardoise triple and the hublot trio. Reading all the briefs before drawing down is what made the umbrella-plus-consolidated-closes shape visible.
- The paired-control rhythm (reproduce known-bad → fix → positive control) ran three times and earned its keep: the cache sort pair, the scanner's planted secret, hublot's C-u receipt. The one place the known-bad would NOT reproduce (nibajo's unsent-paste race) was reported as exactly that, and the guard shipped on the field evidence.
- Sameer's close-time "knock it out rather than filing it" was right — the scanner config work took ~20 minutes with full context and would have cost a future session an hour of re-orientation.

### Uncertain
- What actually triggers the nibajo race (keys' Enter swallowed while the composer ingests a paste) — 247-char and 1.7KB pastes both submitted instantly here on the same CC version the report named. Load, terminal timing, or TUI state are candidates; the 1s settle guards all of them cheaply.

### Risks
- **scan.py exit semantics changed**: exit 2 now means scanner-failure (0 files). Nothing in CI wraps it today, but any future wrapper treating nonzero as "findings" will misread empty scans.
- **ardoise print mode behaviour change**: a bare dir before -p used to be silently ignored (subject ran in /tmp); it now runs there. Corrective, CHANGELOG'd, but a change.
- **The keys settle is a fixed 1s heuristic** — a pathological paste could still outrun it; the field-report pattern will catch it if so.
- **Deglacer edits remain held** (trousse-wazula, waiting on bds-numeri). guriku/jejozo work must wait or land in the moved skill.

### Opportunities
- **Render-verify glance first thing**: one Skill(trousse:ardoise) + Skill(trousse:hublot) + Skill(trousse:skill-forge) load — look for the --cwd doc, the key verb + menus recipe, and the six new checklist rows; all body edits shipped 1.66.1–1.66.5 under the session-cache debt.
- **pezogu's done-test is a fresh-session drive**: from hublot's SKILL.md alone, drive /hooks to a named event without raw tmux — that proves the menus recipe teaches.
- **trousse-vosujo is self-contained**: verify whether the (user) tag is platform-added on Cowork, then soften or scope lint_skill.py's check.
- **trousse-jejozo unblocks when bds-numeri lands**: verify guriku's three slash-command schema errors against live JSONL, corrections land wherever deglacer lives by then.
- **coup-doeil is Sameer's roadmap call**: the cowork-cloud reference works standalone; an orientation-time "which surface am I on?" skill was named once and never boarded.

## For Claudes to come

**A positive control certifies only the layers it exercises.** The sharing scanner was vacuous twice over: the discovery layer (excludes matched against absolute-path parts, so `.claude`-under-home blinded it to every skill it was most often pointed at) and the config layer (the personal-term lists ship empty, leaving four of six detector categories inert). Fixing the first and firing a planted secret produced a genuinely earned green — for the pattern layer only. The second layer surfaced not from any probe but from asking what the control *didn't* contain. For any multi-layer gate, enumerate the layers before banking a control-backed green, and make the gate name its own inert parts (the scanner now prints "categories inert: …" rather than letting an empty list impersonate a clean bill). Corpus-row candidate filed to carte intake rather than written into verification.md directly — close-time is the wrong moment for residence judgement.

**Consolidation is a drawdown shape worth looking for**: when several field reports cluster on one tool, read every brief before claiming any item — the union usually reveals N reports : M bugs with M < N, and one umbrella tactical with consolidated closes beats N sequential passes both for wall-clock and for closing-note coherence.
