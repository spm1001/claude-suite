# Handoff — 2026-08-09

session_id: b324fa3e-8c40-4bbc-88c3-dd66cb2af403
purpose: Wunule night — deja's config-dir blindness mechanism confirmed, fixed, wired, published; the shared index repaired for free
format: fond-v1

## For the next Claude

### Done
- **Cleared yesterday's render-verify debt at open**: fresh-session `Skill(trousse:deglacer)` confirmed all three of the 2026-08-09 daytime edit rounds (cagebo deja section, tekesu Measured-routing block, cumoza dragons) render as written in suite 1.47.3.
- **Pushed the stranded skill-count commit** (905c7c5) — it was local-only, so "rides the next publish" could never fire; the assembler reads origin.
- **Closed trousse-wunule complete** (three actions, one evening): mechanism confirmed from pinned v0.16.9 source — deja's `ClaudeRoot()` follows `CLAUDE_CONFIG_DIR`, and `filepath.WalkDir` Lstats a symlinked root and descends nothing, so the farm's `projects/` symlink read as an empty corpus and the reconciler deleted all 6,092 records (trousse-seluji). Fix is deja's own `DEJA_CLAUDE_ROOT` override, found by reading the source. Proven live under the commis condition, then end-to-end from a real headless `claudec` session: `removed_files=0`, ranked hits identical to the primary lane (trousse-darono). Index repaired as a side effect of the fix probe (468 MB, 6,046 sessions). Launcher `_claude_commis()` in dotfiles now exports the pair together; `~/.claude/handoffs/commis-lane.md` documents why. Skill quirk bullet published in **suite 1.48.1** (trousse-fokocu).
- **Hazard-class sweep verdict: deja was alone.** deglacer hardcodes `~/.claude/projects` and Python's rglob opens symlinked bases; the notes harvester hardcodes it too (why the symlink is load-bearing); réceptionnaire is HOME-isolated with its own cache; garde-manger isn't on tube.
- **Upstream issue drafted, then parked by decision** — `banc/session-search/upstream-issue-draft-symlink-root.md` carries both bugs (symlink-blind root walk; no mass-deletion guard on reconcile) and now carries the decision header: not posting (GitHub anti-AI climate), the public skill text is the community-help channel, revisit only on Sameer's fresh word.

### Reflection
- The repair-by-proof ordering worked exactly as hoped: the index was already wiped, so the 5-minute re-index was owed regardless — running the fix-condition query FIRST made one run pay for mechanism proof, fix proof, and repair.
- Reading the pinned source (90 seconds in the module cache) beat designing around the symptom: `DEJA_CLAUDE_ROOT` was sitting in `claude.go`, purpose-built, and upgraded the fix from blunt (`env -u`) to surgical. The field report's "confirming costs another wipe" assumption dissolved once source + the already-measured matched pair were combined.
- Sameer's queue line (filed 20:59 the night before) outranked the previous handoff's named spine, and that was the right call — a destructive bug beats documentation polish.
- One wrong-pane message ("Rory email") arrived mid-wait and was withdrawn; the mise skill got loaded for nothing. No cost beyond context.

### Risks
- **tmux panes older than 2026-08-09 ~22:30 hold the stale `_claude_commis`** — a `claudec` from one still wipes the index (5-min rebuild, not data loss). Fresh panes are fine; `source ~/.bashrc` cures old ones. Self-heals as panes cycle.
- **The 1.48.1 deglacer bullet is render-unverified** — this session edited the SKILL.md so cannot check the harness's own read of it (frontmatter session-cached). Same debt shape as yesterday, cleared the same way.
- The upstream issue is parked **by decision**, not oversight — do not post it, and don't re-litigate the call; the draft header and wunule's done note both carry it.

### Opportunities
- **Render-verify the 1.48.1 deglacer bullet first thing**: one `Skill(trousse:deglacer)` invocation, look for the "non-default CLAUDE_CONFIG_DIR blinds it" quirk bullet.
- **trousse-jejozo remains the next spine**: verify guriku's three slash-command schema errors against live JSONL, fix the reference — finishes the deglacer accuracy cluster.
- **trousse-vamawu**: fold the two bench-measured writing rules into skill-forge's checklist (source: ~/notes/practices/model-minds/experiment-guidance-style-ab.md).
- Standalones ferupi (attention-narrowing check) and bujuta (scan.py vacuous green) are unclaimed and self-contained.

## For Claudes to come

**When a destructive repro is "too expensive to confirm", look for the ordering that makes the cost pay twice.** The wunule field report left its mechanism as a hypothesis because confirming it meant another index wipe — but the index was already wiped, so the rebuild was a sunk cost waiting to be spent well. Running the *fix-condition* probe first made the mandatory 5-minute re-index simultaneously prove the fix, repair the estate, and (with the pinned source read) confirm the mechanism. The general form: before paying a verification bill, check whether a bill you already owe can be routed through the same run. Second half, same evening: **read the pinned source before designing a workaround for a third-party tool.** The module cache (`~/.local/share/go/pkg/mod/...@v0.16.9`) is on disk, sumdb-verified, and 90 seconds of grep found `DEJA_CLAUDE_ROOT` — a purpose-built escape hatch no amount of black-box probing would have surfaced. Source-reading and behaviour-sampling are different evidence classes (yesterday's lesson), but they *compose*: the matched pair already measured live + the walk code read from source = mechanism confirmed with zero additional risk. And a class-shaped worry ("the symlink farm makes this a hazard class") deflates fast when you classify by discriminator — the class needed all three of config-following + symlink-blind walker + shared mutable artifact, and only deja had them.
